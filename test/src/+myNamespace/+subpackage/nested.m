function result = nested(input)
    % NESTED A dummy function in the nested namespace
    %
    % Syntax:
    %   result = nested(input)
    %
    % Description:
    %   This function takes an input and returns it as the output.
    %
    % Inputs:
    %   input - Any type of input
    %
    % Outputs:
    %   result - The same as the input

    % Simply return the input as the output
    result = input;
end

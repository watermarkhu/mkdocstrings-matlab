%% Introduction
% This is a MATLAB live script example.
% It demonstrates the plain-text live code format (R2025a).

%% Setup
% First we define some variables.

x = 1;
y = 2;

%% Computation
% Now we compute the result.

z = x + y;
disp(z)
